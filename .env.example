# Optional. Without a key Luma starts in clearly labeled Demo Mode.
# Main process only; never use a VITE_ prefix for secrets.
YOUTUBE_API_KEY=
# Set this to your actual repository after publishing, e.g. owner/luma
LUMA_GITHUB_REPOSITORY=
